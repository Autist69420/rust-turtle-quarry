return {
    url = "http://127.0.0.1:8080/",
    websocket_url = "ws://127.0.0.1:8080/ws/",
    chunk_mode = true,
    -- This gets ignored when `chunk_mode` is set to true
    mining = {
        width = 0,
        height = 0,
        depth = 0,
    },
}
